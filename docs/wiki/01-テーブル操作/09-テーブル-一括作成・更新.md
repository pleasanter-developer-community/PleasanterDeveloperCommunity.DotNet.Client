# レコード一括作成・更新 - items/{siteId}/bulkupsert

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [BulkUpsertRecordAsync](#bulkupsertrecordasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

複数のレコードを一括で作成または更新します。キーに基づいて既存レコードの更新または新規レコードの作成を行います。

> **Warning**: プリザンター内部でのUpsert処理は、レコードの存在確認（Get）→作成/更新（Create/Update）の間でロックを取得していません。そのため、**同一Keysで短期間に連続してBulkUpsertを発行すると、存在判定のタイミングによりレースコンディションが発生し、重複作成や例外が発生する可能性があります**。同一キーに対する並列実行が想定される場合は、クライアント側で適切な排他制御を実施してください。

## 対応バージョン

- プリザンター 1.4.6.0以降

## BulkUpsertRecordAsync

```csharp
Task<ApiResponse<BulkUpsertRecordResponse>> BulkUpsertRecordAsync(
    long siteId,
    BulkUpsertRecordRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2                  | #3                 | 型                              | 必須 | 説明                                         |
| ------------------- | ------------------- | ------------------ | ------------------------------- | :--: | -------------------------------------------- |
| `siteId`            |                     |                    | `long`                          | Yes  | サイトID                                     |
| `request`           |                     |                    | `BulkUpsertRecordRequest`       | Yes  | リクエストモデル                             |
|                     | `Keys`              |                    | `List<string>?`                 |      | キー項目（一致判定に使用する項目名）         |
|                     | `KeyNotFoundCreate` |                    | `bool?`                         |      | キーに一致するレコードがない場合に作成するか |
|                     | `Data`              |                    | `List<BulkUpsertRecordData>?`   |      | レコードデータの配列                         |
|                     |                     | `.Title`           | `string?`                       |      | タイトル                                     |
|                     |                     | `.Body`            | `string?`                       |      | 本文                                         |
|                     |                     | `.Status`          | `int?`                          |      | 状態                                         |
|                     |                     | `.Manager`         | `int?`                          |      | 管理者                                       |
|                     |                     | `.Owner`           | `int?`                          |      | 担当者                                       |
|                     |                     | `.CompletionTime`  | `string?`                       |      | 完了日時                                     |
|                     |                     | `.ClassHash`       | `Dictionary<string, string>?`   |      | 分類項目                                     |
|                     |                     | `.NumHash`         | `Dictionary<string, decimal>?`  |      | 数値項目                                     |
|                     |                     | `.DateHash`        | `Dictionary<string, DateTime>?` |      | 日付項目                                     |
|                     |                     | `.DescriptionHash` | `Dictionary<string, string>?`   |      | 説明項目                                     |
|                     |                     | `.CheckHash`       | `Dictionary<string, bool>?`     |      | チェック項目                                 |
| `timeout`           |                     |                    | `TimeSpan?`                     |      | リクエストタイムアウト                       |
| `cancellationToken` |                     |                    | `CancellationToken`             |      | キャンセルトークン                           |

### レスポンス

`ApiResponse<BulkUpsertRecordResponse>` を返却します。

| プロパティ#1 | #2           | 型                          | 説明                 |
| ------------ | ------------ | --------------------------- | -------------------- |
| `StatusCode` |              | `HttpStatusCode`            | HTTPステータスコード |
| `Message`    |              | `string?`                   | レスポンスメッセージ |
| `Response`   |              | `BulkUpsertRecordResponse?` | レスポンスデータ     |
|              | `Id`         | `long`                      | サイトID             |
|              | `StatusCode` | `int`                       | ステータスコード     |
|              | `Message`    | `string?`                   | メッセージ           |
|              | `Created`    | `int?`                      | 作成されたレコード数 |
|              | `Updated`    | `int?`                      | 更新されたレコード数 |
| `IsSuccess`  |              | `bool`                      | 成功したかどうか     |

## 使用例

```csharp
using VehicleVision.Pleasanter.DotNet.Client.Models.Requests.Items;

// リクエストモデルを使用した一括作成・更新
var request = new BulkUpsertRecordRequest
{
    Data = new List<BulkUpsertRecordData>
    {
        new BulkUpsertRecordData
        {
            Title = "レコード1",
            ClassHash = new Dictionary<string, string> { { "ClassA", "値1" } }
        },
        new BulkUpsertRecordData
        {
            Title = "レコード2",
            ClassHash = new Dictionary<string, string> { { "ClassA", "値2" } }
        }
    },
    Keys = new List<string> { "ClassA" }
};

var result = await client.BulkUpsertRecordAsync(siteId: 456, request: request);
```

## 関連ドキュメント

- [タイムアウトとキャンセル](../00-共通/タイムアウトとキャンセル.md) - タイムアウトとキャンセルトークンの使い方
- [レスポンスの処理](../00-共通/レスポンスの処理.md) - APIレスポンスの処理方法
- [レコード作成・更新](./08-テーブル-作成・更新.md)
- [レコード一括削除](./11-テーブル-一括削除.md)
