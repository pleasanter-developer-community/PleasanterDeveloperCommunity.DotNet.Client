# レコード一括削除 - items/{siteId}/bulkdelete

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [BulkDeleteRecordAsync](#bulkdeleterecordasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
    - [レコードIDを指定して削除](#レコードidを指定して削除)
    - [フィルタ条件で絞って削除](#フィルタ条件で絞って削除)
    - [フィルタ条件で絞って物理削除](#フィルタ条件で絞って物理削除)
    - [すべてのレコードを削除](#すべてのレコードを削除)
- [注意事項](#注意事項)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

指定したサイトIDのテーブルからレコードを一括削除します。

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## BulkDeleteRecordAsync

```csharp
Task<ApiResponse<BulkDeleteRecordResponse>> BulkDeleteRecordAsync(
    long siteId,
    BulkDeleteRecordRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2               | #3                   | 型                                   | 必須 | 説明                       |
| ------------------- | ---------------- | -------------------- | ------------------------------------ | :--: | -------------------------- |
| `siteId`            |                  |                      | `long`                               | Yes  | サイトID                   |
| `request`           |                  |                      | `BulkDeleteRecordRequest`            | Yes  | リクエストモデル           |
|                     | `Selected`       |                      | `List<long>?`                        |      | 削除対象のレコードID一覧   |
|                     | `All`            |                      | `bool?`                              |      | 全件削除フラグ             |
|                     | `PhysicalDelete` |                      | `bool?`                              |      | 物理削除フラグ             |
|                     | `View`           |                      | `View?`                              |      | ビュー設定（フィルタ条件） |
|                     |                  | `Incomplete`         | `bool?`                              |      | 未完了のみ                 |
|                     |                  | `Own`                | `bool?`                              |      | 自分が担当のみ             |
|                     |                  | `NearCompletionTime` | `bool?`                              |      | 期限が近いのみ             |
|                     |                  | `Delay`              | `bool?`                              |      | 遅延しているのみ           |
|                     |                  | `Overdue`            | `bool?`                              |      | 期限超過のみ               |
|                     |                  | `Search`             | `string?`                            |      | 全文検索キーワード         |
|                     |                  | `ColumnFilterHash`   | `Dictionary<string, string>?`        |      | 列フィルタ設定             |
|                     |                  | `ColumnSorterHash`   | `Dictionary<string, SortOrderType>?` |      | ソート設定                 |
| `timeout`           |                  |                      | `TimeSpan?`                          |      | リクエストタイムアウト     |
| `cancellationToken` |                  |                      | `CancellationToken`                  |      | キャンセルトークン         |

### レスポンス

`ApiResponse<BulkDeleteRecordResponse>` を返却します。

| プロパティ#1 | #2           | 型                          | 説明                     |
| ------------ | ------------ | --------------------------- | ------------------------ |
| `StatusCode` |              | `HttpStatusCode`            | HTTPステータスコード     |
| `Message`    |              | `string?`                   | レスポンスメッセージ     |
| `Response`   |              | `BulkDeleteRecordResponse?` | レスポンスデータ         |
|              | `Id`         | `long`                      | 一括削除対象のサイトID   |
|              | `StatusCode` | `int`                       | ステータスコード         |
|              | `Message`    | `string?`                   | メッセージ（削除件数等） |
| `IsSuccess`  |              | `bool`                      | 成功したかどうか         |

## 使用例

### レコードIDを指定して削除

```csharp
using VehicleVision.Pleasanter.DotNet.Client.Models.Requests.Items;

// リクエストモデルを使用した一括削除
var request = new BulkDeleteRecordRequest
{
    Selected = new List<long> { 135, 376 }
};

var result = await client.BulkDeleteRecordAsync(siteId: 12345, request: request);

if (result.IsSuccess)
{
    Console.WriteLine($"サイトID: {result.Response?.Id}");
    Console.WriteLine($"メッセージ: {result.Message}");
}
```

### フィルタ条件で絞って削除

```csharp
using VehicleVision.Pleasanter.DotNet.Client.Models.Requests.Items;

var request = new BulkDeleteRecordRequest
{
    View = new View
    {
        ColumnFilterHash = new Dictionary<string, string>
        {
            { "Status", "[100,910]" }
        }
    }
};

var result = await client.BulkDeleteRecordAsync(siteId: 12345, request: request);
```

### フィルタ条件で絞って物理削除

```csharp
using VehicleVision.Pleasanter.DotNet.Client.Models.Requests.Items;

var request = new BulkDeleteRecordRequest
{
    View = new View
    {
        ColumnFilterHash = new Dictionary<string, string>
        {
            { "Status", "[100,910]" }
        }
    },
    PhysicalDelete = true
};

var result = await client.BulkDeleteRecordAsync(siteId: 12345, request: request);
```

### すべてのレコードを削除

```csharp
using VehicleVision.Pleasanter.DotNet.Client.Models.Requests.Items;

var request = new BulkDeleteRecordRequest
{
    All = true
};

var result = await client.BulkDeleteRecordAsync(siteId: 12345, request: request);
```

## 注意事項

- **この操作は元に戻せません。** 削除レコードや削除条件に細心の注意を払ってください。
- 削除前に `GetRecordsAsync` で削除対象のレコードを取得し、確認することを推奨します。
- `PhysicalDelete` を `true` にした場合、変更履歴およびごみ箱からも削除され、復元できなくなります。
- `Selected`、`View`、`All` のいずれかを指定する必要があります。

## 関連ドキュメント

- [タイムアウトとキャンセル](../00-共通/タイムアウトとキャンセル.md) - タイムアウトとキャンセルトークンの使い方
- [レスポンスの処理](../00-共通/レスポンスの処理.md) - APIレスポンスの処理方法
- [レコード削除](./10-レコード-削除.md)
- [レコード取得（複数）](<./04-テーブル-取得(複数).md>)
