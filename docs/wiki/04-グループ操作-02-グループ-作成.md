# グループ作成 - groups/create

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [CreateGroupAsync](#creategroupasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

APIを使用して新しいグループを作成します。

**制限事項**: テナント管理者権限が必要です。

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## CreateGroupAsync

```csharp
Task<ApiResponse<CreateGroupResponse>> CreateGroupAsync(
    CreateGroupRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2             | #3        | 型                          | 必須 | 説明                   |
| ------------------- | -------------- | --------- | --------------------------- | :--: | ---------------------- |
| `request`           |                |           | `CreateGroupRequest`        | Yes  | リクエストモデル       |
|                     | `GroupName`    |           | `string?`                   |      | グループ名             |
|                     | `Body`         |           | `string?`                   |      | 内容                   |
|                     | `Disabled`     |           | `bool?`                     |      | 無効                   |
|                     | `GroupMembers` |           | `List<GroupMemberRequest>?` |      | グループメンバー       |
|                     |                | `.DeptId` | `int?`                      |      | 組織ID                 |
|                     |                | `.UserId` | `int?`                      |      | ユーザID               |
|                     |                | `.Admin`  | `bool?`                     |      | 管理者フラグ           |
| `timeout`           |                |           | `TimeSpan?`                 |      | リクエストタイムアウト |
| `cancellationToken` |                |           | `CancellationToken`         |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | #2   | 型                 | 説明                 |
| ------------ | ---- | ------------------ | -------------------- |
| `StatusCode` |      | `HttpStatusCode`   | HTTPステータスコード |
| `Message`    |      | `string?`          | メッセージ           |
| `IsSuccess`  |      | `bool`             | 成功したかどうか     |
| `Response`   |      | `CreateGroupData?` | レスポンスデータ     |
|              | `Id` | `long`             | 作成されたグループID |

## 使用例

```csharp
using PleasanterDeveloperCommunity.DotNet.Client.Models.Requests.Groups;

// シンプルなグループ作成
var request = new CreateGroupRequest
{
    GroupName = "開発チーム",
    Body = "開発メンバーのグループ"
};

var result = await client.CreateGroupAsync(request);

if (result.IsSuccess)
{
    Console.WriteLine($"作成されたグループID: {result.Response?.Id}");
}

// メンバーを含むグループ作成
var requestWithMembers = new CreateGroupRequest
{
    GroupName = "プロジェクトチーム",
    Body = "プロジェクトメンバー",
    GroupMembers = new List<GroupMemberRequest>
    {
        new GroupMemberRequest { UserId = 1, Admin = true },
        new GroupMemberRequest { UserId = 2, Admin = false },
        new GroupMemberRequest { DeptId = 1 }  // 組織全体を追加
    }
};

var resultWithMembers = await client.CreateGroupAsync(requestWithMembers);
```

## 関連ドキュメント

- [タイムアウトとキャンセル](00-タイムアウトとキャンセル) - タイムアウトとキャンセルトークンの使い方
- [レスポンスの処理](00-レスポンスの処理) - APIレスポンスの処理方法
