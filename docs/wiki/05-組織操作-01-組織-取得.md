# 組織の取得 - depts/get

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [GetDeptsAsync](#getdeptsasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
    - [全組織を取得](#全組織を取得)
    - [フィルタを使用](#フィルタを使用)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

APIを使用して組織一覧を取得します。

- テナント管理者権限が必要です

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## GetDeptsAsync

```csharp
Task<ApiResponse<GetDeptsResponse>> GetDeptsAsync(
    GetDeptsRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2       | 型                  | 必須 | 説明                   |
| ------------------- | -------- | ------------------- | :--: | ---------------------- |
| `request`           |          | `GetDeptsRequest`   | Yes  | リクエストモデル       |
|                     | `View`   | `View?`             |      | ビュー設定             |
|                     | `Offset` | `int?`              |      | 取得開始位置           |
| `timeout`           |          | `TimeSpan?`         |      | リクエストタイムアウト |
| `cancellationToken` |          | `CancellationToken` |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | #2           | #3             | 型                  | 説明             |
| ------------ | ------------ | -------------- | ------------------- | ---------------- |
| `StatusCode` |              |                | `HttpStatusCode`    | ステータスコード |
| `Message`    |              |                | `string?`           | メッセージ       |
| `IsSuccess`  |              |                | `bool`              | 成功したかどうか |
| `Response`   |              |                | `GetDeptsResponse?` | レスポンスデータ |
|              | `Data`       |                | `List<DeptData>?`   | 組織データリスト |
|              |              | `.TenantId`    | `int`               | テナントID       |
|              |              | `.DeptId`      | `int`               | 組織ID           |
|              |              | `.Ver`         | `int`               | バージョン       |
|              |              | `.DeptCode`    | `string?`           | 組織コード       |
|              |              | `.DeptName`    | `string?`           | 組織名           |
|              |              | `.Body`        | `string?`           | 内容             |
|              |              | `.Disabled`    | `bool`              | 無効フラグ       |
|              |              | `.Comments`    | `string?`           | コメント         |
|              |              | `.Creator`     | `int`               | 作成者           |
|              |              | `.Updator`     | `int`               | 更新者           |
|              |              | `.CreatedTime` | `DateTime?`         | 作成日時         |
|              |              | `.UpdatedTime` | `DateTime?`         | 更新日時         |
|              | `TotalCount` |                | `int`               | 合計件数         |

## 使用例

### 全組織を取得

```csharp
using PleasanterDeveloperCommunity.DotNet.Client.Models.Requests.Depts;

var request = new GetDeptsRequest();
var result = await client.GetDeptsAsync(request);

if (result.IsSuccess)
{
    foreach (var dept in result.Response?.Data ?? new List<DeptData>())
    {
        Console.WriteLine($"組織: {dept.DeptName} ({dept.DeptCode})");
    }
}
```

### フィルタを使用

```csharp
using PleasanterDeveloperCommunity.DotNet.Client.Models.Requests.Depts;

var request = new GetDeptsRequest
{
    Offset = 0,
    View = new View
    {
        ColumnFilterHash = new Dictionary<string, string>
        {
            { "Disabled", "false" }
        }
    }
};

var result = await client.GetDeptsAsync(request);
```

## 関連ドキュメント

- [タイムアウトとキャンセル](00-タイムアウトとキャンセル)
- [レスポンスの処理](00-レスポンスの処理)
