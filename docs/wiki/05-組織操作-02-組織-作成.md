# 組織の作成 - depts/create

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [CreateDeptAsync](#createdeptasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
    - [シンプルな組織作成](#シンプルな組織作成)
    - [リクエストモデルを使用した作成](#リクエストモデルを使用した作成)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

APIを使用して新しい組織を作成します。

- テナント管理者権限が必要です

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## CreateDeptAsync

```csharp
Task<ApiResponse<CreateDeptResponse>> CreateDeptAsync(
    CreateDeptRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2         | 型                  | 必須 | 説明                   |
| ------------------- | ---------- | ------------------- | :--: | ---------------------- |
| `request`           |            | `CreateDeptRequest` | Yes  | リクエストモデル       |
|                     | `DeptCode` | `string?`           |      | 組織コード             |
|                     | `DeptName` | `string?`           |      | 組織名                 |
|                     | `Body`     | `string?`           |      | 内容                   |
|                     | `Disabled` | `bool?`             |      | 無効                   |
| `timeout`           |            | `TimeSpan?`         |      | リクエストタイムアウト |
| `cancellationToken` |            | `CancellationToken` |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | #2           | 型                    | 説明             |
| ------------ | ------------ | --------------------- | ---------------- |
| `StatusCode` |              | `HttpStatusCode`      | ステータスコード |
| `Message`    |              | `string?`             | メッセージ       |
| `IsSuccess`  |              | `bool`                | 成功したかどうか |
| `Response`   |              | `CreateDeptResponse?` | レスポンスデータ |
|              | `Id`         | `int`                 | 作成された組織ID |
|              | `StatusCode` | `int`                 | ステータスコード |
|              | `Message`    | `string?`             | メッセージ       |

## 使用例

### シンプルな組織作成

```csharp
var result = await client.CreateDeptAsync(
    deptCode: "DEV001",
    deptName: "開発部",
    body: "ソフトウェア開発部門");

if (result.IsSuccess)
{
    Console.WriteLine($"作成された組織ID: {result.Response?.Id}");
}
```

### リクエストモデルを使用した作成

```csharp
using PleasanterDeveloperCommunity.DotNet.Client.Models.Requests.Depts;

var request = new CreateDeptRequest
{
    DeptCode = "SALES001",
    DeptName = "営業部",
    Body = "営業部門",
    Disabled = false
};

var result = await client.CreateDeptAsync(request);

if (result.IsSuccess)
{
    Console.WriteLine($"作成された組織ID: {result.Response?.Id}");
}
```

## 関連ドキュメント

- [タイムアウトとキャンセル](00-タイムアウトとキャンセル)
- [レスポンスの処理](00-レスポンスの処理)
