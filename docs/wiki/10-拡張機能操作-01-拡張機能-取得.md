# 拡張機能の取得 - extensions/get

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [制限事項](#制限事項)
- [GetExtensionsAsync](#getextensionsasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
    - [拡張機能一覧を取得](#拡張機能一覧を取得)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

APIを使用して拡張機能一覧を取得します。

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## 制限事項

- サービス管理者権限が必要です

## GetExtensionsAsync

```csharp
Task<ApiResponse<GetExtensionsResponse>> GetExtensionsAsync(
    GetExtensionsRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2  | 型                     | 必須 | 説明                   |
| ------------------- | --- | ---------------------- | :--: | ---------------------- |
| `request`           |     | `GetExtensionsRequest` | Yes  | リクエストモデル       |
| `timeout`           |     | `TimeSpan?`            |      | リクエストタイムアウト |
| `cancellationToken` |     | `CancellationToken`    |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | #2     | #3                      | 型                        | 説明                 |
| ------------ | ------ | ----------------------- | ------------------------- | -------------------- |
| `StatusCode` |        |                         | `int`                     | ステータスコード     |
| `Message`    |        |                         | `string?`                 | メッセージ           |
| `Response`   |        |                         | `ExtensionsResponseData?` | レスポンスデータ     |
|              | `Data` |                         | `List<ExtensionData>?`    | 拡張機能データリスト |
|              |        | `.ExtensionId`          | `long`                    | 拡張機能ID           |
|              |        | `.ExtensionType`        | `string?`                 | 種類                 |
|              |        | `.ExtensionName`        | `string?`                 | 拡張機能名           |
|              |        | `.ExtensionDescription` | `string?`                 | 説明                 |
|              |        | `.ExtensionSettings`    | `string?`                 | 設定                 |
|              |        | `.Disabled`             | `bool`                    | 無効フラグ           |
|              |        | `.Creator`              | `long`                    | 作成者               |
|              |        | `.Updator`              | `long`                    | 更新者               |
|              |        | `.CreatedTime`          | `DateTime`                | 作成日時             |
|              |        | `.UpdatedTime`          | `DateTime`                | 更新日時             |

## 使用例

### 拡張機能一覧を取得

```csharp
using PleasanterDeveloperCommunity.DotNet.Client.Models.Requests.Extensions;

var request = new GetExtensionsRequest();
var result = await client.GetExtensionsAsync(request: request);

if (result.Data?.StatusCode == 200)
{
    foreach (var ext in result.Data.Response?.Data ?? new List<ExtensionData>())
    {
        Console.WriteLine($"拡張機能: {ext.ExtensionName} ({ext.ExtensionType})");
    }
}
```

## 関連ドキュメント

- [タイムアウトとキャンセル](00-タイムアウトとキャンセル) - タイムアウトとキャンセルトークンの使い方
- [拡張機能の作成](10-拡張機能操作-02-拡張機能-作成)
- [拡張機能の更新](10-拡張機能操作-03-拡張機能-更新)
- [拡張機能の削除](10-拡張機能操作-04-拡張機能-削除)
