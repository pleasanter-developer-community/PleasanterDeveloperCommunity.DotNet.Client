# バイナリのストリームアップロード - binaries/upload (Bearer認証)

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#%E6%A6%82%E8%A6%81)
- [対応バージョン](#%E5%AF%BE%E5%BF%9C%E3%83%90%E3%83%BC%E3%82%B8%E3%83%A7%E3%83%B3)
- [特徴](#%E7%89%B9%E5%BE%B4)
- [UploadBinaryStreamAsync (ストリーム版)](#uploadbinarystreamasync-%E3%82%B9%E3%83%88%E3%83%AA%E3%83%BC%E3%83%A0%E7%89%88)
    - [パラメータ](#%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF)
    - [レスポンス](#%E3%83%AC%E3%82%B9%E3%83%9D%E3%83%B3%E3%82%B9)
- [UploadBinaryStreamAsync (バイト配列版)](#uploadbinarystreamasync-%E3%83%90%E3%82%A4%E3%83%88%E9%85%8D%E5%88%97%E7%89%88)
    - [パラメータ](#%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF-1)
    - [レスポンス](#%E3%83%AC%E3%82%B9%E3%83%9D%E3%83%B3%E3%82%B9-1)
- [UploadBinaryStreamFromFileAsync](#uploadbinarystreamfromfileasync)
    - [パラメータ](#%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF-2)
    - [レスポンス](#%E3%83%AC%E3%82%B9%E3%83%9D%E3%83%B3%E3%82%B9-2)
- [UpdateBinaryStreamAsync (ストリーム版)](#updatebinarystreamasync-%E3%82%B9%E3%83%88%E3%83%AA%E3%83%BC%E3%83%A0%E7%89%88)
    - [パラメータ](#%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF-3)
    - [レスポンス](#%E3%83%AC%E3%82%B9%E3%83%9D%E3%83%B3%E3%82%B9-3)
- [UpdateBinaryStreamAsync (バイト配列版)](#updatebinarystreamasync-%E3%83%90%E3%82%A4%E3%83%88%E9%85%8D%E5%88%97%E7%89%88)
    - [パラメータ](#%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF-4)
    - [レスポンス](#%E3%83%AC%E3%82%B9%E3%83%9D%E3%83%B3%E3%82%B9-4)
- [UploadBinaryChunkAsync](#uploadbinarychunkasync)
    - [パラメータ](#%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF-5)
    - [レスポンス](#%E3%83%AC%E3%82%B9%E3%83%9D%E3%83%B3%E3%82%B9-5)
- [UpdateBinaryChunkAsync](#updatebinarychunkasync)
    - [パラメータ](#%E3%83%91%E3%83%A9%E3%83%A1%E3%83%BC%E3%82%BF-6)
    - [レスポンス](#%E3%83%AC%E3%82%B9%E3%83%9D%E3%83%B3%E3%82%B9-6)
- [使用例](#%E4%BD%BF%E7%94%A8%E4%BE%8B)
    - [ファイルをストリームでアップロード](#%E3%83%95%E3%82%A1%E3%82%A4%E3%83%AB%E3%82%92%E3%82%B9%E3%83%88%E3%83%AA%E3%83%BC%E3%83%A0%E3%81%A7%E3%82%A2%E3%83%83%E3%83%97%E3%83%AD%E3%83%BC%E3%83%89)
    - [ファイルパスを指定してアップロード](#%E3%83%95%E3%82%A1%E3%82%A4%E3%83%AB%E3%83%91%E3%82%B9%E3%82%92%E6%8C%87%E5%AE%9A%E3%81%97%E3%81%A6%E3%82%A2%E3%83%83%E3%83%97%E3%83%AD%E3%83%BC%E3%83%89)
    - [既存ファイルを更新](#%E6%97%A2%E5%AD%98%E3%83%95%E3%82%A1%E3%82%A4%E3%83%AB%E3%82%92%E6%9B%B4%E6%96%B0)
    - [分割アップロード（チャンク）](#%E5%88%86%E5%89%B2%E3%82%A2%E3%83%83%E3%83%97%E3%83%AD%E3%83%BC%E3%83%89%E3%83%81%E3%83%A3%E3%83%B3%E3%82%AF)
- [関連ドキュメント](#%E9%96%A2%E9%80%A3%E3%83%89%E3%82%AD%E3%83%A5%E3%83%A1%E3%83%B3%E3%83%88)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

Bearer認証を使用してファイルをマルチパートフォームデータとしてアップロードします。Base64エンコードが不要なため、大きなファイルのアップロードに適しています。また、分割（チャンク）アップロードにも対応しています。

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## 特徴

- **Bearer認証**: `Authorization: Bearer {ApiKey}` ヘッダーを使用
- **マルチパートフォームデータ**: Base64エンコード不要で効率的
- **分割アップロード**: Content-Rangeヘッダーによる大容量ファイルの分割転送
- **ファイルハッシュ検証**: 最終チャンクでのデータ整合性確認

## UploadBinaryStreamAsync (ストリーム版)

```csharp
Task<ApiResponse<UploadBinaryResponse>> UploadBinaryStreamAsync(
    long siteId,
    Stream fileStream,
    string fileName,
    string? contentType = null,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | 型                  | 必須 | 説明                   |
| ------------------- | ------------------- | :--: | ---------------------- |
| `siteId`            | `long`              | Yes  | サイトID               |
| `fileStream`        | `Stream`            | Yes  | ファイルストリーム     |
| `fileName`          | `string`            | Yes  | ファイル名             |
| `contentType`       | `string?`           |      | コンテンツタイプ       |
| `timeout`           | `TimeSpan?`         |      | リクエストタイムアウト |
| `cancellationToken` | `CancellationToken` |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | #2     | 型                          | 説明                             |
| ------------ | ------ | --------------------------- | -------------------------------- |
| `StatusCode` |        | `int`                       | ステータスコード                 |
| `Message`    |        | `string?`                   | メッセージ                       |
| `Response`   |        | `UploadBinaryResponseData?` | レスポンスデータ                 |
|              | `Guid` | `string?`                   | アップロードされたファイルのGUID |

## UploadBinaryStreamAsync (バイト配列版)

```csharp
Task<ApiResponse<UploadBinaryResponse>> UploadBinaryStreamAsync(
    long siteId,
    byte[] fileData,
    string fileName,
    string? contentType = null,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | 型                  | 必須 | 説明                         |
| ------------------- | ------------------- | :--: | ---------------------------- |
| `siteId`            | `long`              | Yes  | サイトID                     |
| `fileData`          | `byte[]`            | Yes  | ファイルデータ（バイト配列） |
| `fileName`          | `string`            | Yes  | ファイル名                   |
| `contentType`       | `string?`           |      | コンテンツタイプ             |
| `timeout`           | `TimeSpan?`         |      | リクエストタイムアウト       |
| `cancellationToken` | `CancellationToken` |      | キャンセルトークン           |

### レスポンス

[UploadBinaryStreamAsync (ストリーム版) のレスポンス](#レスポンス)と同じ

## UploadBinaryStreamFromFileAsync

```csharp
Task<ApiResponse<UploadBinaryResponse>> UploadBinaryStreamFromFileAsync(
    long siteId,
    string filePath,
    string? contentType = null,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | 型                  | 必須 | 説明                   |
| ------------------- | ------------------- | :--: | ---------------------- |
| `siteId`            | `long`              | Yes  | サイトID               |
| `filePath`          | `string`            | Yes  | ファイルパス           |
| `contentType`       | `string?`           |      | コンテンツタイプ       |
| `timeout`           | `TimeSpan?`         |      | リクエストタイムアウト |
| `cancellationToken` | `CancellationToken` |      | キャンセルトークン     |

### レスポンス

[UploadBinaryStreamAsync (ストリーム版) のレスポンス](#レスポンス)と同じ

## UpdateBinaryStreamAsync (ストリーム版)

```csharp
Task<ApiResponse<UploadBinaryResponse>> UpdateBinaryStreamAsync(
    string guid,
    Stream fileStream,
    string fileName,
    string? contentType = null,
    bool overwrite = false,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | 型                  | 必須 | 説明                              |
| ------------------- | ------------------- | :--: | --------------------------------- |
| `guid`              | `string`            | Yes  | 更新対象のファイルGUID            |
| `fileStream`        | `Stream`            | Yes  | ファイルストリーム                |
| `fileName`          | `string`            | Yes  | ファイル名                        |
| `contentType`       | `string?`           |      | コンテンツタイプ                  |
| `overwrite`         | `bool`              |      | 上書きモード（デフォルト: false） |
| `timeout`           | `TimeSpan?`         |      | リクエストタイムアウト            |
| `cancellationToken` | `CancellationToken` |      | キャンセルトークン                |

### レスポンス

[UploadBinaryStreamAsync (ストリーム版) のレスポンス](#レスポンス)と同じ

## UpdateBinaryStreamAsync (バイト配列版)

```csharp
Task<ApiResponse<UploadBinaryResponse>> UpdateBinaryStreamAsync(
    string guid,
    byte[] fileData,
    string fileName,
    string? contentType = null,
    bool overwrite = false,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | 型                  | 必須 | 説明                              |
| ------------------- | ------------------- | :--: | --------------------------------- |
| `guid`              | `string`            | Yes  | 更新対象のファイルGUID            |
| `fileData`          | `byte[]`            | Yes  | ファイルデータ（バイト配列）      |
| `fileName`          | `string`            | Yes  | ファイル名                        |
| `contentType`       | `string?`           |      | コンテンツタイプ                  |
| `overwrite`         | `bool`              |      | 上書きモード（デフォルト: false） |
| `timeout`           | `TimeSpan?`         |      | リクエストタイムアウト            |
| `cancellationToken` | `CancellationToken` |      | キャンセルトークン                |

### レスポンス

[UploadBinaryStreamAsync (ストリーム版) のレスポンス](#レスポンス)と同じ

## UploadBinaryChunkAsync

```csharp
Task<ApiResponse<UploadBinaryResponse>> UploadBinaryChunkAsync(
    long siteId,
    byte[] chunkData,
    string fileName,
    long rangeFrom,
    long rangeTo,
    long rangeTotal,
    string? contentType = null,
    string? fileHash = null,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | 型                  | 必須 | 説明                                       |
| ------------------- | ------------------- | :--: | ------------------------------------------ |
| `siteId`            | `long`              | Yes  | サイトID                                   |
| `chunkData`         | `byte[]`            | Yes  | チャンクデータ                             |
| `fileName`          | `string`            | Yes  | ファイル名                                 |
| `rangeFrom`         | `long`              | Yes  | Content-Rangeの開始バイト位置              |
| `rangeTo`           | `long`              | Yes  | Content-Rangeの終了バイト位置              |
| `rangeTotal`        | `long`              | Yes  | ファイル全体のバイトサイズ                 |
| `contentType`       | `string?`           |      | コンテンツタイプ                           |
| `fileHash`          | `string?`           |      | ファイルハッシュ（最終チャンク時に検証用） |
| `timeout`           | `TimeSpan?`         |      | リクエストタイムアウト                     |
| `cancellationToken` | `CancellationToken` |      | キャンセルトークン                         |

### レスポンス

[UploadBinaryStreamAsync (ストリーム版) のレスポンス](#レスポンス)と同じ

## UpdateBinaryChunkAsync

```csharp
Task<ApiResponse<UploadBinaryResponse>> UpdateBinaryChunkAsync(
    string guid,
    byte[] chunkData,
    string fileName,
    long rangeFrom,
    long rangeTo,
    long rangeTotal,
    string? contentType = null,
    bool overwrite = false,
    string? fileHash = null,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | 型                  | 必須 | 説明                                       |
| ------------------- | ------------------- | :--: | ------------------------------------------ |
| `guid`              | `string`            | Yes  | 更新対象のファイルGUID                     |
| `chunkData`         | `byte[]`            | Yes  | チャンクデータ                             |
| `fileName`          | `string`            | Yes  | ファイル名                                 |
| `rangeFrom`         | `long`              | Yes  | Content-Rangeの開始バイト位置              |
| `rangeTo`           | `long`              | Yes  | Content-Rangeの終了バイト位置              |
| `rangeTotal`        | `long`              | Yes  | ファイル全体のバイトサイズ                 |
| `contentType`       | `string?`           |      | コンテンツタイプ                           |
| `overwrite`         | `bool`              |      | 上書きモード（デフォルト: false）          |
| `fileHash`          | `string?`           |      | ファイルハッシュ（最終チャンク時に検証用） |
| `timeout`           | `TimeSpan?`         |      | リクエストタイムアウト                     |
| `cancellationToken` | `CancellationToken` |      | キャンセルトークン                         |

### レスポンス

[UploadBinaryStreamAsync (ストリーム版) のレスポンス](#レスポンス)と同じ

## 使用例

### ファイルをストリームでアップロード

```csharp
using var fileStream = File.OpenRead("path/to/large-file.zip");
var result = await client.UploadBinaryStreamAsync(
    siteId: 123,
    fileStream: fileStream,
    fileName: "large-file.zip",
    contentType: "application/zip");

if (result.Data?.StatusCode == 200)
{
    Console.WriteLine($"アップロード完了。GUID: {result.Data.Response?.Guid}");
}
```

### ファイルパスを指定してアップロード

```csharp
var result = await client.UploadBinaryStreamFromFileAsync(
    siteId: 123,
    filePath: "C:/Documents/report.pdf",
    contentType: "application/pdf");
```

### 既存ファイルを更新

```csharp
var result = await client.UpdateBinaryStreamAsync(
    guid: "existing-file-guid",
    fileData: newFileBytes,
    fileName: "updated-file.pdf",
    contentType: "application/pdf",
    overwrite: true);  // 同じGUIDで上書き
```

### 分割アップロード（チャンク）

大きなファイルを複数のチャンクに分割してアップロードする例：

```csharp
var filePath = "path/to/very-large-file.zip";
var fileInfo = new FileInfo(filePath);
var totalSize = fileInfo.Length;
var chunkSize = 1024 * 1024;  // 1MB

using var fileStream = File.OpenRead(filePath);
var buffer = new byte[chunkSize];
var offset = 0L;

while (offset < totalSize)
{
    var bytesToRead = (int)Math.Min(chunkSize, totalSize - offset);
    var bytesRead = await fileStream.ReadAsync(buffer, 0, bytesToRead);
    var chunkData = new byte[bytesRead];
    Array.Copy(buffer, chunkData, bytesRead);

    var rangeFrom = offset;
    var rangeTo = offset + bytesRead - 1;

    // 最終チャンクの場合はファイルハッシュを指定（オプション）
    string? fileHash = null;
    if (rangeTo == totalSize - 1)
    {
        // 必要に応じてファイルハッシュを計算
        // fileHash = ComputeSha256Hash(filePath);
    }

    var result = await client.UploadBinaryChunkAsync(
        siteId: 123,
        chunkData: chunkData,
        fileName: Path.GetFileName(filePath),
        rangeFrom: rangeFrom,
        rangeTo: rangeTo,
        rangeTotal: totalSize,
        contentType: "application/zip",
        fileHash: fileHash);

    if (result.Data?.StatusCode != 200)
    {
        Console.WriteLine($"チャンクアップロード失敗: {result.Data?.Message}");
        break;
    }

    offset += bytesRead;
    Console.WriteLine($"Progress: {offset}/{totalSize} ({(double)offset / totalSize * 100:F1}%)");
}

Console.WriteLine("アップロード完了");
```

## 関連ドキュメント

- [タイムアウトとキャンセル](../00-共通/タイムアウトとキャンセル.md)
- [レスポンスの処理](../00-共通/レスポンスの処理.md)
- [添付ファイルの取得（Base64形式）](<./01-バイナリ-取得(Base64).md>)
- [バイナリストリームの取得](./02-バイナリ-ストリーム取得.md)
- [バイナリのアップロード](./03-バイナリ-アップロード.md)
