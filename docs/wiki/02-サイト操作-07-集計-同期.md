# 集計の同期 - sites/{siteId}/synchronizesummaries

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [制限事項](#制限事項)
- [SynchronizeSummariesAsync](#synchronizesummariesasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
    - [集計を同期](#集計を同期)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

APIを使用してサイトの集計を同期します。

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## 制限事項

- サイトの管理権限が必要です

## SynchronizeSummariesAsync

```csharp
Task<ApiResponse<SynchronizeSummariesResponse>> SynchronizeSummariesAsync(
    long siteId,
    SynchronizeSummariesRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2           | 型                            | 必須 | 説明                   |
| ------------------- | ------------ | ----------------------------- | :--: | ---------------------- |
| `siteId`            |              | `long`                        | Yes  | サイトID               |
| `request`           |              | `SynchronizeSummariesRequest` | Yes  | リクエストモデル       |
|                     | `ApiVersion` | `string?`                     |      | APIバージョン          |
|                     | `ApiKey`     | `string?`                     |      | APIキー                |
| `timeout`           |              | `TimeSpan?`                   |      | リクエストタイムアウト |
| `cancellationToken` |              | `CancellationToken`           |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | 型        | 説明             |
| ------------ | --------- | ---------------- |
| `Id`         | `long`    | サイトID         |
| `StatusCode` | `int`     | ステータスコード |
| `Message`    | `string?` | メッセージ       |

## 使用例

### 集計を同期

```csharp
using PleasanterDeveloperCommunity.DotNet.Client.Models.Requests.Sites;

var request = new SynchronizeSummariesRequest();
var result = await client.SynchronizeSummariesAsync(siteId: 123, request: request);

if (result.Data?.StatusCode == 200)
{
    Console.WriteLine("集計を同期しました");
}
```

## 関連ドキュメント

- [タイムアウトとキャンセル](00-タイムアウトとキャンセル) - タイムアウトとキャンセルトークンの使い方
- [レスポンスの処理](00-レスポンスの処理) - レスポンスの処理方法
