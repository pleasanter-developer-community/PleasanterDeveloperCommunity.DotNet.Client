# グループ更新 - groups/{groupId}/update

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [UpdateGroupAsync](#updategroupasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

APIを使用して既存のグループ情報を更新します。

**制限事項**: テナント管理者権限が必要です。

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## UpdateGroupAsync

```csharp
Task<ApiResponse<UpdateGroupResponse>> UpdateGroupAsync(
    long groupId,
    UpdateGroupRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2             | #3        | 型                          | 必須 | 説明                   |
| ------------------- | -------------- | --------- | --------------------------- | :--: | ---------------------- |
| `groupId`           |                |           | `long`                      | Yes  | グループID             |
| `request`           |                |           | `UpdateGroupRequest`        | Yes  | リクエストモデル       |
|                     | `GroupName`    |           | `string?`                   |      | グループ名             |
|                     | `Body`         |           | `string?`                   |      | 内容                   |
|                     | `Disabled`     |           | `bool?`                     |      | 無効                   |
|                     | `GroupMembers` |           | `List<GroupMemberRequest>?` |      | グループメンバー       |
|                     |                | `.DeptId` | `int?`                      |      | 組織ID                 |
|                     |                | `.UserId` | `int?`                      |      | ユーザID               |
|                     |                | `.Admin`  | `bool?`                     |      | 管理者フラグ           |
| `timeout`           |                |           | `TimeSpan?`                 |      | リクエストタイムアウト |
| `cancellationToken` |                |           | `CancellationToken`         |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | #2   | 型                 | 説明                 |
| ------------ | ---- | ------------------ | -------------------- |
| `StatusCode` |      | `HttpStatusCode`   | HTTPステータスコード |
| `Message`    |      | `string?`          | メッセージ           |
| `IsSuccess`  |      | `bool`             | 成功したかどうか     |
| `Response`   |      | `UpdateGroupData?` | レスポンスデータ     |
|              | `Id` | `long`             | 更新されたグループID |

## 使用例

```csharp
using PleasanterDeveloperCommunity.DotNet.Client.Models.Requests.Groups;

// リクエストモデルを使用した更新
var request = new UpdateGroupRequest
{
    GroupName = "更新後のグループ名",
    Body = "更新後の説明",
    GroupMembers = new List<GroupMemberRequest>
    {
        new GroupMemberRequest { UserId = 1, Admin = true }
    }
};

var result = await client.UpdateGroupAsync(groupId: 123, request: request);

if (result.IsSuccess)
{
    Console.WriteLine("グループ情報を更新しました");
}
```

## 関連ドキュメント

- [タイムアウトとキャンセル](00-タイムアウトとキャンセル) - タイムアウトとキャンセルトークンの使い方
- [レスポンスの処理](00-レスポンスの処理) - APIレスポンスの処理方法
