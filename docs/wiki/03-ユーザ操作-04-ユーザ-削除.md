# ユーザの削除 - users/{userId}/delete

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [DeleteUserAsync](#deleteuserasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

APIを使用してユーザを削除します。

- テナント管理者権限が必要です
- 自分自身は削除できません

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## DeleteUserAsync

```csharp
Task<ApiResponse<DeleteUserResponse>> DeleteUserAsync(
    long userId,
    DeleteUserRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2  | 型                  | 必須 | 説明                   |
| ------------------- | --- | ------------------- | :--: | ---------------------- |
| `userId`            |     | `long`              | Yes  | ユーザID               |
| `request`           |     | `DeleteUserRequest` | Yes  | リクエストモデル       |
| `timeout`           |     | `TimeSpan?`         |      | リクエストタイムアウト |
| `cancellationToken` |     | `CancellationToken` |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | #2   | 型                | 説明               |
| ------------ | ---- | ----------------- | ------------------ |
| `StatusCode` |      | `HttpStatusCode`  | ステータスコード   |
| `Message`    |      | `string?`         | メッセージ         |
| `IsSuccess`  |      | `bool`            | 成功したかどうか   |
| `Response`   |      | `DeleteUserData?` | レスポンスデータ   |
|              | `Id` | `long`            | 削除されたユーザID |

## 使用例

```csharp
using PleasanterDeveloperCommunity.DotNet.Client.Models.Requests.Users;

var request = new DeleteUserRequest();
var result = await client.DeleteUserAsync(123, request);

if (result.IsSuccess)
{
    Console.WriteLine("ユーザを削除しました");
}
```

## 関連ドキュメント

- [タイムアウトとキャンセル](00-タイムアウトとキャンセル)
- [レスポンスの処理](00-レスポンスの処理)
