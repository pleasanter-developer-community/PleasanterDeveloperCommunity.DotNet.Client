# 拡張機能の更新 - extensions/{extensionId}/update

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [制限事項](#制限事項)
- [UpdateExtensionAsync](#updateextensionasync)
    - [パラメータ](#パラメータ)
    - [レスポンス](#レスポンス)
- [使用例](#使用例)
    - [拡張機能を更新](#拡張機能を更新)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

APIを使用して既存の拡張機能を更新します。

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## 制限事項

- サービス管理者権限が必要です

## UpdateExtensionAsync

```csharp
Task<ApiResponse<UpdateExtensionResponse>> UpdateExtensionAsync(
    long extensionId,
    UpdateExtensionRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2                     | 型                       | 必須 | 説明                   |
| ------------------- | ---------------------- | ------------------------ | :--: | ---------------------- |
| `extensionId`       |                        | `long`                   | Yes  | 拡張機能ID             |
| `request`           |                        | `UpdateExtensionRequest` | Yes  | リクエストモデル       |
|                     | `ExtensionType`        | `string?`                |      | 種類                   |
|                     | `ExtensionName`        | `string?`                |      | 拡張機能名             |
|                     | `ExtensionDescription` | `string?`                |      | 説明                   |
|                     | `ExtensionSettings`    | `string?`                |      | 設定                   |
|                     | `Disabled`             | `bool?`                  |      | 無効                   |
| `timeout`           |                        | `TimeSpan?`              |      | リクエストタイムアウト |
| `cancellationToken` |                        | `CancellationToken`      |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | 型        | 説明                 |
| ------------ | --------- | -------------------- |
| `StatusCode` | `int`     | ステータスコード     |
| `Message`    | `string?` | メッセージ           |
| `Id`         | `long`    | 更新された拡張機能ID |

## 使用例

### 拡張機能を更新

```csharp
using PleasanterDeveloperCommunity.DotNet.Client.Models.Requests.Extensions;

var request = new UpdateExtensionRequest
{
    ExtensionName = "更新後の拡張機能名",
    ExtensionSettings = "SELECT Id, Title FROM Items"
};

var result = await client.UpdateExtensionAsync(extensionId: 123, request: request);

if (result.Data?.StatusCode == 200)
{
    Console.WriteLine("拡張機能を更新しました");
}
```

## 関連ドキュメント

- [タイムアウトとキャンセル](00-タイムアウトとキャンセル) - タイムアウトとキャンセルトークンの使い方
- [拡張機能の取得](10-拡張機能操作-01-拡張機能-取得)
- [拡張機能の作成](10-拡張機能操作-02-拡張機能-作成)
- [拡張機能の削除](10-拡張機能操作-04-拡張機能-削除)
