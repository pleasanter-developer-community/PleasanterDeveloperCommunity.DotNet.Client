# 拡張機能の作成 - extensions/create

<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->

- [概要](#概要)
- [対応バージョン](#対応バージョン)
- [制限事項](#制限事項)
- [CreateExtensionAsync](#createextensionasync)
        - [パラメータ](#パラメータ)
        - [レスポンス](#レスポンス)
- [使用例](#使用例)
        - [拡張機能を作成](#拡張機能を作成)
- [関連ドキュメント](#関連ドキュメント)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## 概要

APIを使用して新しい拡張機能を作成します。

## 対応バージョン

- プリザンター 1.3.13.0以降
    - このライブラリの制約によるもの

## 制限事項

- サービス管理者権限が必要です

## CreateExtensionAsync

```csharp
Task<ApiResponse<CreateExtensionResponse>> CreateExtensionAsync(
    CreateExtensionRequest request,
    TimeSpan? timeout = null,
    CancellationToken cancellationToken = default)
```

### パラメータ

| プロパティ#1        | #2                     | 型                       | 必須 | 説明                   |
| ------------------- | ---------------------- | ------------------------ | :--: | ---------------------- |
| `request`           |                        | `CreateExtensionRequest` | Yes  | リクエストモデル       |
|                     | `ExtensionType`        | `string?`                |      | 種類（Sql等）          |
|                     | `ExtensionName`        | `string?`                |      | 拡張機能名             |
|                     | `ExtensionDescription` | `string?`                |      | 説明                   |
|                     | `ExtensionSettings`    | `string?`                |      | 設定内容               |
|                     | `Disabled`             | `bool?`                  |      | 無効                   |
| `timeout`           |                        | `TimeSpan?`              |      | リクエストタイムアウト |
| `cancellationToken` |                        | `CancellationToken`      |      | キャンセルトークン     |

### レスポンス

| プロパティ#1 | 型        | 説明                 |
| ------------ | --------- | -------------------- |
| `StatusCode` | `int`     | ステータスコード     |
| `Message`    | `string?` | メッセージ           |
| `Id`         | `long`    | 作成された拡張機能ID |

## 使用例

### 拡張機能を作成

```csharp
using VehicleVision.Pleasanter.DotNet.Client.Models.Requests.Extensions;

var request = new CreateExtensionRequest
{
    ExtensionType = "Sql",
    ExtensionName = "カスタムSQL",
    ExtensionSettings = "SELECT * FROM Items",
    ExtensionDescription = "テスト用SQL拡張機能"
};

var result = await client.CreateExtensionAsync(request: request);

if (result.Data?.StatusCode == 200)
{
    Console.WriteLine($"作成された拡張機能ID: {result.Data.Id}");
}
```

## 関連ドキュメント

- [タイムアウトとキャンセル](00-タイムアウトとキャンセル) - タイムアウトとキャンセルトークンの使い方
- [拡張機能の取得](10-拡張機能操作-01-拡張機能-取得)
- [拡張機能の更新](10-拡張機能操作-03-拡張機能-更新)
- [拡張機能の削除](10-拡張機能操作-04-拡張機能-削除)
